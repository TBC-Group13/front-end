name: Frontend CI/CD Pipeline

on:
  push:
    branches:
      - main

jobs:
  build-and-deploy:
    name: Build and Deploy Frontend
    runs-on: ubuntu-latest

    steps:
    - name: Checkout Code
      uses: actions/checkout@v3

    - name: Set up Node.js
      uses: actions/setup-node@v3
      with:
        node-version: '22.10.0'

    - name: Install Dependencies
      run: |
        npm install
      working-directory: ${{ github.workspace }}

    - name: Build Frontend
      run: |
        npm run build
      working-directory: ${{ github.workspace }}

    - name: Deploy Frontend
      env:
        SSH_PRIVATE_KEY: ${{ secrets.SSH_PRIVATE_KEY }}
      run: |
<<<<<<< HEAD
        mkdir -p ${{ github.workspace }}/ssh
        echo "$SSH_PRIVATE_KEY" > ${{ github.workspace }}/ssh/id_rsa
        chmod 600 ${{ github.workspace }}/ssh/id_rsa
        ssh-keyscan -H 172.31.40.45 > ${{ github.workspace }}/ssh/known_hosts
        ssh -o StrictHostKeyChecking=no -i ${{ github.workspace }}/ssh/id_rsa ubuntu@172.31.40.45 "sudo systemctl reload nginx"
=======
        mkdir -p ~/.ssh
        echo "$SSH_PRIVATE_KEY" > ~/.ssh/id_rsa
        chmod 600 ~/.ssh/id_rsa
        ssh-keyscan -H 172.31.40.45 >> ~/.ssh/known_hosts
        ssh -o StrictHostKeyChecking=no -i ~/.ssh/id_rsa ubuntu@172.31.40.45 "sudo systemctl reload nginx"

>>>>>>> c1829d5 (Add/Update Frontend CI/CD pipeline)
